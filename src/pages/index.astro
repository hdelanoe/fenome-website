---
// src/pages/index.astro
import BaseLayout from '../layouts/BaseLayout.astro';
import "../styles/global.css";

import HeroSection from '../components/HeroSection.astro';
import AboutSection from '../components/AboutSection.astro';
import ServicesSection from '../components/ServicesSection.astro';
import PortfolioSlider from '../components/PortfolioSlider.astro';
import ClientsSection from '../components/ClientsSection.astro';
 // ...

const pageTitle = "Fénome - Créateur d'expériences événementielles";
const pageDescription = "Accompagnement événementiel, direction artistique, scénographie, mapping vidéo, dispositifs interactifs et plus encore. Découvrez Fénome.";
---
<BaseLayout title={pageTitle} description={pageDescription}>
    <!-- C'est ici qu'on ajoutera toutes nos sections plus tard -->
    <HeroSection title="Créateur d'expériences événementielles immersives" subtitle="Créateur d'expériences événementielles immersives" />

    {/* Exemple d'utilisation des couleurs et polices */}
    {/* --- Zone pour les autres sections --- */}
   <div class="space-y-16 md:space-y-24"> {/* Ajoute de l'espace entre les futures sections */}

        {/* Section À Propos (placeholder) */}
        <AboutSection />

        <!-- Section Services -->
        <ServicesSection />

        {/* === Portfolio Section === */}
        <PortfolioSlider />
        {/* ========================= */}

        {/* === Clients Section === */}
        <ClientsSection />
        {/* ===================== */}
    </div>

</BaseLayout>